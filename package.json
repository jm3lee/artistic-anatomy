{
  "name": "artistic-anatomy",
  "private": true,
  "version": "0.0.0",
  "description": "",
  "workspaces": [
    "src/v3"
  ],
  "scripts": {
    "start": "npm run dev -w v3",
    "lint": "npm run lint --workspaces",
    "test": "npm test --workspaces",
    "format": "prettier --write ./src/v3 package.json"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
